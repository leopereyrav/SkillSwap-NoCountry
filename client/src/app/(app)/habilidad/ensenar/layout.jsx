export const metadata = {
    title: 'Enseñar Habilidades',
  };

  export default function layoutEnseñar({ children }) {
      return (
        <>
        {children}
        </>
      );
  }